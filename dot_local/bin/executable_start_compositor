#!/usr/bin/zsh -l

export XDG_CURRENT_DESKTOP=river

[ -f .river.log ] && mv .river.log .river.log.1

exec river -log-level debug >.river.log 2>&1

# river 0.4+
#[ -f .wm.log ] && mv .wm.log .wm.log.1
#WM_CMD='argen >.wm.log 2>&1'
#exec river -c "exec $WM_CMD" -log-level debug >.river.log 2>&1
